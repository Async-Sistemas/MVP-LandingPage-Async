export default defineAppConfig({
  ui: {
    colors: {
      primary: 'async',
      secondary: 'grafite',
      neutral: 'slate'
    },
    button: {
      slots: {
        base: 'font-medium'
      }
    }
  }
})
